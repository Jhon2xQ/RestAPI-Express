export interface CreateUserDTO {
  id: number;
  lastName: string;
  firstName: string;
  email: string;
  password: string;
}

export interface PublicUserDTO {
  id: number;
  lastName: string;
  firstName: String;
  email: string;
}
